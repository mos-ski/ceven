import { MobileShell } from "@/components/caregiver/mobile-shell";

export default function CaregiverV1Layout({ children }: { children: React.ReactNode }) {
 return <MobileShell>{children}</MobileShell>;
}
